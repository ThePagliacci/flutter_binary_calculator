import 'package:flutter/material.dart';


class ascii_conversion extends StatelessWidget {
  const ascii_conversion({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("ascii conversion page");
  }
}
